<%= button_to "create campaign", admin_campaign_index_path, method: :post %>

<ul>
  <% @campaigns.each do |campaign| %>
      <li><%= Time.at(campaign.date).strftime("%A, %d %b %Y %H:%M %p") %></li>
  <% end %>
</ul>