<div class="landing">

  <div class="landing__welcome-image">
  </div>

  <div class="landing__container">
    <div class="landing__welcome-title">
    </div>

    <div class="landing__message">
      <div class="landing__message-image">We heart you</div>
      <div class="landing__message-safe">We want you to stay safe!</div>
      <div class="landing__message-alert">Sign up for emergency alerts!</div>
      <div class="landing__message-context">Our new system can now send location based alerts. This means if you get an alert, the sytem has intelligently determined that you NEED the details to stay safe, because you are within the affected area. This also means you won't recieve alerts that don't affect you.</div>
    </div>

    <div class="landing__register-button">
      <button class="landing__button">Sign Up</button>
    </div>

    <div class="landing__login-button">
      <button class="landing__button">Login</button>
    </div>
  </div>
</div>

<h1>WEATHER</h1>

<ul>
  <% @weather.each do |weather| %>
      <li><%= weather %></li>
  <% end %>
</ul>

<h1>TSUNAMIS</h1>

<ul>
  <% @tsunami.each do |tsunami| %>
      <li><%= tsunami.as_json %></li>
  <% end %>
</ul>


<h1>RIVERS</h1>

<ul>
  <% @rivers[0..4].each do |river| %>
      <li><%= river.as_json %></li>
  <% end %>
</ul>

<h1>FIRES</h1>

<ul>
  <% @fires.each do |fire| %>
      <li><%= fire.as_json %></li>
  <% end %>
</ul>

<h1>EARTHQUAKES</h1>

<ul>
  <% @earthquakes.each do |quake| %>
      <li><%= quake %></li>
  <% end %>
</ul>
