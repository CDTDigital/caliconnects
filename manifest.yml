---
applications:
- name: garage-ruby-starter
  host: garage-ruby-starter
  path: .
  memory: 256MB
  instances: 1
  buildpack: https://github.com/cloudfoundry/ruby-buildpack
  command: bundle exec rake cf:on_first_instance db:migrate && bundle exec rails s -p $PORT
